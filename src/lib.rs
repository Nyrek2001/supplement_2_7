/// Library root module.
/// 
/// Exposes the `number` module and the `run` function which is called from `main.rs`.
pub mod number;

/// Starts the CLI application. Actual CLI logic will be implemented later.
pub fn run() {
    println!("Numeric Type Reinterpretation CLI");
    // CLI logic will be implemented here later
}
